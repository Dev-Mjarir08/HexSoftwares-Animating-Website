{
  "name": "noir-luxury-clothing",
  "version": "1.0.0",
  "description": "NOIR — Awwwards-level luxury clothing brand website. Built with Tailwind CSS (CLI), Vanilla JS, GSAP, ScrollTrigger, Lenis, SplitType and Swiper.js.",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i ./src/tailwind-input.css -o ./assets/css/style.css --minify",
    "watch:css": "tailwindcss -i ./src/tailwind-input.css -o ./assets/css/style.css --watch",
    "build": "npm run build:css"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.13"
  }
}
